---
  - name: Install Parity Playbook
    hosts: k8s
    connection: ssh
    gather_facts: True
    tags: provisioning
    
    tasks:
    
    - name: docker up
      service:
        name: docker
        state: started
    
    - name: Pull parity to the OSTree backend
      atomic_image:
        name: parity/parity
        state: latest
        backend: ostree
    
    - name: parity up
      become: yes
      atomic_container:
        name: etcd
        image: parity/parity
        backend: ostree
        state: latest
        mode: system