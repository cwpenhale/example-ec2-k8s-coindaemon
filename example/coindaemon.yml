---
  - name: Install Parity Playbook
    hosts: k8s
    connection: ssh
    gather_facts: True
    tags: provisioning
    
    tasks:
    
    - name: Install Parity
      atomic_image:
          name: parity/parity
          state: latest
          started: yes