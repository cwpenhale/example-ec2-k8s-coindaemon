---
  - name: Install Parity Playbook
    hosts: k8s
    connection: ssh
    gather_facts: False
    tags: installation
    
    tasks:
    
    - name: docker up
      service:
        name: docker
        state: started
    
    - name: Pull parity to the OSTree backend
      atomic_image:
        name: parity/parity:v1.11.0-ci0
        state: latest
        backend: docker
    
    - name: parity image up
      become: yes
      atomic_container:
        name: parity
        image: parity/parity
        state: latest
        mode: system
        backend: ostree
        values:
          - ui-interface=all
          - jsonrpc-interface=all