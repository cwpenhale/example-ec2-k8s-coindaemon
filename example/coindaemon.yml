---
  - name: Install Parity Playbook
    hosts: k8s
    connection: ssh
    gather_facts: False
    tags: installation
    
    tasks:
    
    - name: docker up
      service:
        name: docker
        state: started
    

           
    